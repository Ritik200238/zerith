// Twitter card image — same 1200x630 design as OpenGraph.
// Re-exported so Next.js wires it into the twitter.images metadata block.

export { default } from "./opengraph-image";
export { runtime, contentType, size, alt } from "./opengraph-image";
